<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
        https://www.springframework.org/schema/beans/spring-beans.xsd">

    <!--
        Hands-on 4: Country bean with property injection.
        scope="prototype" demonstrates that Spring creates a NEW instance
        each time context.getBean("country") is called.
        Change to scope="singleton" (or remove scope) to see singleton behaviour –
        constructor is called only ONCE even with multiple getBean() calls.
    -->
    <bean id="country" class="com.cognizant.springlearn.model.Country" scope="prototype">
        <property name="code" value="IN" />
        <property name="name" value="India" />
    </bean>

    <!-- Hands-on 6 – individual country beans -->
    <bean id="in" class="com.cognizant.springlearn.model.Country">
        <property name="code" value="IN" />
        <property name="name" value="India" />
    </bean>

    <bean id="us" class="com.cognizant.springlearn.model.Country">
        <property name="code" value="US" />
        <property name="name" value="United States" />
    </bean>

    <bean id="de" class="com.cognizant.springlearn.model.Country">
        <property name="code" value="DE" />
        <property name="name" value="Germany" />
    </bean>

    <bean id="jp" class="com.cognizant.springlearn.model.Country">
        <property name="code" value="JP" />
        <property name="name" value="Japan" />
    </bean>

    <!--
        Hands-on 6 – ArrayList of all four countries.
        <list> holds ordered references to individual country beans via <ref>.
        Retrieved in code as: context.getBean("countryList", ArrayList.class)
    -->
    <bean id="countryList" class="java.util.ArrayList">
        <constructor-arg>
            <list>
                <ref bean="in"/>
                <ref bean="us"/>
                <ref bean="de"/>
                <ref bean="jp"/>
            </list>
        </constructor-arg>
    </bean>

</beans>
